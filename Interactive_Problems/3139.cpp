#include <bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL)
#define ll long long
#define vll vector<ll>

void solve(){
    ll n;
    cin>>n;
    vll ans(n);
    vll prev[n];
    vll next[n];
    
}

int main(){
    fastio;
    int tt=1;
    cin>>tt;
    for(int i=1;i<=tt;i++){
        solve();
    }
    return 0;
}